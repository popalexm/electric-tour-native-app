<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textViewStyle">@style/TextViewStyle</item>
        <item name="bottomSheetDialogTheme">@style/BottomSheetDialogTheme</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="TextViewStyle" parent="android:Widget.TextView">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="TextViewStyleHint" parent="android:Widget.TextView">
        <item name="android:textColorHint">@color/colorLightGrey</item>
        <item name="android:textStyle">italic</item>
    </style>

    <style name="SwitchStyle" parent="Theme.AppCompat.NoActionBar">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/colorAccent</item>
        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/colorPrimaryDark</item>
        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/colorPrimaryDark</item>

    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="searchIcon">@drawable/ic_search_white_24dp</item>
        <item name="searchHintIcon">@drawable/ic_search_white_24dp</item>
        <item name="closeIcon">@drawable/ic_close_white_24dp</item>
    </style>

    <style name="SearchViewStyle.ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="autoCompleteTextViewStyle">@style/AutoCompleteTextView.SearchView</item>
    </style>

    <style name="AutoCompleteTextView.SearchView" parent="Widget.AppCompat.AutoCompleteTextView">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/ModalStyle</item>
    </style>

    <style name="ModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/dialog_background</item>
    </style>

    <style name="markerTextStyle">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="clusterMarkerTextStyle">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="buttonFilterStyle">
        <item name="backgroundTint">@color/colorPrimaryDark</item>
        <item name="cornerRadius">14dp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="RoundedButtonStyleConfirm" parent="Widget.MaterialComponents.Button.Icon">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">12sp</item>
        <item name="backgroundTint">@color/colorBlue</item>
        <item name="cornerRadius">16dp</item>
        <item name="iconTintMode">multiply</item>
        <item name="rippleColor">@color/colorWhite</item>
    </style>

    <style name="RoundedButtonStyleCancel" parent="Widget.MaterialComponents.Button.Icon">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">12sp</item>
        <item name="backgroundTint">@color/colorDeepOrange</item>
        <item name="cornerRadius">16dp</item>
        <item name="iconTintMode">multiply</item>
        <item name="rippleColor">@color/colorWhite</item>
    </style>

    <style name="RoundedButtonStyleDelete" parent="Widget.MaterialComponents.Button.Icon">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">12sp</item>
        <item name="backgroundTint">@color/colorRed</item>
        <item name="cornerRadius">16dp</item>
        <item name="iconTintMode">multiply</item>
        <item name="rippleColor">@color/colorWhite</item>
    </style>

    <style name="MyLocationButtonStyle">
        <item name="backgroundTint">@android:color/white</item>
        <item name="fabSize">mini</item>
        <item name="rippleColor">@color/colorBlue</item>
        <item name="android:elevation">@dimen/margin_medium</item>

    </style>

</resources>
